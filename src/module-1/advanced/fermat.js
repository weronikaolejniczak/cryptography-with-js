/**
 * Algorithm which checks the primality using Fermat's test (missing: 0 pts, implementation up to 5 pts)
 *
 * Input: natural number 1 < n, natural number N
 *
 * Output: 'yes' if the natural number a was a composite number witness of n; if after N
 * we have not obtained a composite number certificate, we return 'no conclusion'
 *
 * WARNING: choose witness in the range 1 < a < n which are co-prime with n and random.
 */

function fermat(n, N) {
    console.log('Algorithm which checks the primality using Fermat\'s test');
}

module.exports = fermat;
